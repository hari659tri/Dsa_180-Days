#include<iostream>
using namespace std;

 int main(){
    int a=8;
    int b=9;
     if(a<b){
        cout<<"inside if";
     }
     else if(b>a){
        cout<<"inside else if";
     }
     else{
        cout<<"inside else";
     }
   
   /* 
    how eise-if executed is 
    if block is true then it executed and rest of ignored like a comment 
    if ->if is false  and else if is true then else if executed and rest is ignored 
    and if non of them true then else is run but at a time only one is run  and other ignored even condition also not checked if if is true ..and for else if also  
   
   
   
   
   */




 }  